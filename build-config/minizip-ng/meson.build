minizip_proj = subproject('minizip-ng')

if py_installation
    minizip_dep = minizip_proj.get_variable('minizip_ng_dep').as_static()
else
    minizip_dep = minizip_proj.get_variable('minizip_ng_dep')
endif